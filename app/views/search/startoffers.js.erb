
$("#startoffers").html("<%= escape_javascript(render partial: 'search/startoffers')%>").show();

$("#close-offer").on("click",function(id){
  $("#startoffers").hide();
  $("#search").show();
});
$("#oneclick").on("click",function(id){
  var product_id = "<%= @product.id %>";
  var user_id = "<%= @user.id %>";
/*  .ajax({
      url: 'create',
      data: {messages_attributes: {product_id: product_id, title: title}}
      type: "POST",
      success: function(data) {
        alert("Successful");
      },
      failure: function() {
        alert("Unsuccessful");
      }
    });*/
  window.location.href = "/offers/chat/" + product_id;
});


 $(".image-checkbox").each(function () {
                    if ($(this).find('input[type="checkbox"]').first().attr("checked")) {
                        $(this).addClass('image-checkbox-checked');
                    } else {
                        $(this).removeClass('image-checkbox-checked');
                    }
                });

                // sync the state to the input
                $(".image-checkbox").on("click", function (e) {
                    $(this).toggleClass('image-checkbox-checked');
                    var $checkbox = $(this).find('input[type="checkbox"]');
                    $checkbox.prop("checked", !$checkbox.prop("checked"))

                    e.preventDefault();
                });
